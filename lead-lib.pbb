|{ include, cwd, ... }|
|{
    languages ? { },
    ...
}|
let
    lang = include (cwd / "lang" / "lang.pbb") {
        languages = languages;
    };

    tk = include (cwd / "tools" / "toolkit.pbb");
in
{
    build = lang.build;
    merge = lang.merge;
    tk = tk;
    lang = lang.export;
}