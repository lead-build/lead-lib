|{ cwd, include, ... }|
let
    lang_c = include "${cwd}/lib/lang_c.pbb";

    app = {
        srcs = [
            "${cwd}/main.c",
            "${cwd}/lib/lib.c",
        ];
        incs = [
            "${cwd}/lib",
        ];
    };
in
(lang_c.build "${cwd}/my_app" app)