|{ cwd, ... }|
let
    mod = | l |
    let
        ll = l l;
    in
    {
        app = "my_app";
        arch = "x86_64";
        outdir = "${cwd}/out";
        outfile = ll.outdir / (ll.app + "-" + ll.arch);
    };

    res = mod mod;
in
res.outfile
