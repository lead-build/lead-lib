|{ pb, ... }|
let

    merge_target = |{
        subdir = tgta_subdir ? [],
        ...
    }|
    |{
        subdir = tgtb_subdir ? [],
        ...
    }|
    {
        subdir = tgta_subdir ++ tgtb_subdir;
    };
in
{
    merge_target = merge_target;
}